# nixos-configuration

My **default** configuration.nix file located /etc/nixos/

To reconfigure a NixOs machine, run command

```

nixos-rebuild switch

````

This will make the configution in the file to be activated and made default.
